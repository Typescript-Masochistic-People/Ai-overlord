export let token: string = ""; //paste you bot token here, To get you discord bot token go here: https://discord.com/developers/applications
export let prefix: string = "/"; //change your prefix, The default is "/", but you can change this to whatever you want it to be
export let owners: string[] = [""]; //place your discord ID here, This is not needed but you can place it here. You can have more then 1 user here
export let dbName: string = "MainDB"; //SQL database name, You do not need to change this
export let ytApiKey: string = ""; // Your YTapi key you can grab from the google cloud console
export let botPrefix: string = ""; //Same as Prefix but meant for the music commands
export let botClient: string = "client"; // Bot client defined as client